<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Marketplace App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <nav class="bg-gray-800 text-white shadow-lg mb-8">
      <div class="container mx-auto px-4">
        <div class="flex justify-between items-center py-4">
          <a class="text-xl font-bold tracking-tight" href="/"><%= content_for?(:title) ? yield(:title) : "Marketplace" %></a>
          <div class="hidden md:flex items-center space-x-4">
            <% if authenticated? %>
              <%= link_to 'All Listings', listings_path, class: 'px-3 py-2 rounded-md text-sm font-medium hover:bg-gray-700' %>
              <%= link_to 'My Listings', mine_listings_path, class: 'px-3 py-2 rounded-md text-sm font-medium hover:bg-gray-700' %>
              <%= link_to 'Profile', profile_path, class: 'px-3 py-2 rounded-md text-sm font-medium hover:bg-gray-700' %>
            <% end %>
          </div>
          <div class="flex items-center gap-4">
            <% if authenticated? %>
              <span class="text-sm text-gray-300 hidden sm:inline">Hi <%= current_user.first_name.presence || current_user.username || current_user.email_address %></span>
              <%= button_to 'Sign out', session_path, method: :delete, class: 'bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-3 rounded-md text-sm transition' %>
            <% else %>
              <%= link_to 'Sign in', new_session_path, class: 'bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-3 rounded-md text-sm transition' %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <div class="container mx-auto px-4">
      <div class="py-8 mb-8 bg-white rounded-lg shadow-md">
        <div class="container-lg px-4">
          <h2 class="text-4xl font-extrabold text-gray-900 mb-2">NU Campus Marketplace</h2>
          <p class="text-gray-600 text-lg">Buy and sell items across campus</p>
            <% unless authenticated? %>
              <div class="mt-4">
                <%= link_to 'Create an account', new_user_path, class: 'bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded-lg shadow-lg hover:shadow-xl transition-transform transform hover:-translate-y-px' %>
              </div>
            <% end %>
        </div>
      </div>
      <% flash.each do |key, message| %>
        <% alert_class = key == 'notice' ? 'bg-green-100 border-green-500 text-green-700' : 'bg-red-100 border-red-500 text-red-700' %>
        <div class="<%= alert_class %> border-l-4 p-4 mb-4" role="alert">
          <p><%= message %></p>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
