<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Marketplace App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <!-- Bootstrap CSS (via CDN for Milestone 0) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUa6mY5Y2v1sA7U6q6o7+Y5Y1j6iZ6b6q6K6s6Q6s6Q6s6Q6s6Q6s6Q6s6Q" crossorigin="anonymous">
    <%= javascript_importmap_tags %>
    <!-- Bootstrap JS bundle (includes Popper) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeo5sV7Yh5Y1j6iZ6b6q6K6s6Q6s6Q6s6Q6s6Q6s6Q6s6Q" crossorigin="anonymous" defer></script>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
      <div class="container">
        <a class="navbar-brand" href="/"><%= content_for?(:title) ? yield(:title) : "Marketplace" %></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <% if authenticated? %>
              <li class="nav-item"><%= link_to 'All Listings', listings_path, class: 'nav-link' %></li>
              <li class="nav-item"><%= link_to 'My Listings', mine_listings_path, class: 'nav-link' %></li>
              <li class="nav-item"><%= link_to 'Profile', profile_path, class: 'nav-link' %></li>
            <% end %>
          </ul>
          <div class="d-flex align-items-center gap-2">
            <% if authenticated? %>
              <span class="navbar-text text-white-50 small"><%= current_user.email_address %></span>
              <%= button_to 'Sign out', session_path, method: :delete, class: 'btn btn-outline-light btn-sm', form: { class: 'd-inline' } %>
            <% else %>
              <%= link_to 'Sign in', new_session_path, class: 'btn btn-outline-light btn-sm' %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <div class="container">
      <div class="py-4 mb-4 bg-white rounded shadow-sm">
        <div class="container-lg">
          <h2 class="mb-0">NU Campus Marketplace</h2>
          <p class="text-muted mb-0">Buy and sell items across campus</p>
            <% if authenticated? %>
              <div class="mt-3">
                <%= link_to 'Create listing', new_listing_path, class: 'btn btn-success' %>
              </div>
            <% else %>
              <div class="mt-3">
                <%= link_to 'Create an account', new_user_path, class: 'btn btn-primary' %>
              </div>
            <% end %>
        </div>
      </div>
      <% flash.each do |key, message| %>
        <div class="alert alert-<%= key == 'notice' ? 'success' : 'danger' %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
