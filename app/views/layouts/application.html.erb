<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title><%= content_for(:title) || "Marketplace App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="<%= image_path('logo.png') %>" type="image/png">
    <link rel="apple-touch-icon" href="<%= image_path('logo.png') %>">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:z-50 m-3 px-4 py-2 bg-white shadow-md rounded-md">Skip to main content</a>
    <nav class="bg-gray-800 text-white shadow-md">
      <div class="container mx-auto px-4">
        <div class="flex flex-col gap-4 py-4 md:flex-row md:items-center md:justify-between">
          <a class="flex items-center gap-3 hover:text-blue-200 transition" href="/">
            <%= image_tag "logo.png",
                          alt: "NU Campus Marketplace logo",
                          size: "40x40",
                          class: "h-10 w-10 rounded-full border border-white/20 object-contain shadow-sm" %>
            <div class="flex flex-col">
              <h1 class="text-xl font-bold tracking-tight leading-tight text-gray-100">NU Campus Marketplace</h1>
              <span class="text-sm text-gray-300">Buy and sell items across campus</span>
            </div>
          </a>
          <div class="hidden md:flex items-center space-x-4">
            <% if authenticated? %>
              <%= link_to 'All Listings', listings_path, class: 'px-3 py-2 rounded-md text-sm font-medium hover:bg-gray-700' %>
              <%= link_to 'My Listings', mine_listings_path, class: 'px-3 py-2 rounded-md text-sm font-medium hover:bg-gray-700' %>
              <%= link_to 'Profile', profile_path, class: 'px-3 py-2 rounded-md text-sm font-medium hover:bg-gray-700' %>
            <% end %>
          </div>
          <div class="flex items-center gap-3">
            <% if authenticated? %>
              <span class="text-sm text-gray-300 hidden sm:inline">Hi <%= current_user.first_name.presence || current_user.username || current_user.email_address %></span>
              <%= button_to 'Sign out', session_path, method: :delete, class: 'bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-3 rounded-md text-sm transition' %>
            <% else %>
              <%= link_to 'Sign in', new_session_path, class: 'bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-3 rounded-md text-sm transition' %>
              <%= link_to 'Create account', new_user_path, class: 'hidden sm:inline-flex items-center justify-center rounded-md border border-white/30 px-3 py-2 text-sm font-semibold text-white transition hover:border-white hover:bg-white/10' %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <main class="container mx-auto px-4 py-8" id="main-content">
      <% flash.each do |key, message| %>
        <% alert_class = key == 'notice' ? 'bg-green-100 border-green-500 text-green-700' : 'bg-red-100 border-red-500 text-red-700' %>
        <div class="<%= alert_class %> border-l-4 p-4 mb-4" role="alert">
          <p><%= message %></p>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
