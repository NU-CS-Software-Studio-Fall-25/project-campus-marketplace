<%= turbo_stream.replace dom_id(@listing, :hide_toggle),
                         partial: "hidden_listings/toggle",
                         locals: { listing: @listing } %>

<%= turbo_stream.remove dom_id(@listing) %>

<%= turbo_stream.update "flash-messages",
                        partial: "shared/flash" %>
